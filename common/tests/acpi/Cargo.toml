# @file Cargo.toml
#
# Cargo package manifest for the ACPI ASL source-level validation tests
#
# SPDX-License-Identifier: MIT

[package]
name = "acpi-tests"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
publish = false

[dev-dependencies]
logos = "0.15"
