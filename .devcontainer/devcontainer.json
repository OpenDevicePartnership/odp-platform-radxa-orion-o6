{
    "name": "Radxa Orion O6 Build Environment",
    "build": {
        "dockerfile": "Dockerfile",
        "context": ".."
    },
    "workspaceFolder": "/workspace",
    "workspaceMount": "source=${localWorkspaceFolder},target=/workspace,type=bind,consistency=cached",
    "onCreateCommand": "git config --global --add safe.directory /workspace; git submodule update --init --recursive 2>/dev/null || echo 'Warning: git submodule update --init --recursive failed; please run it manually to investigate.'",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-vscode.cpptools",
                "ms-vscode.makefile-tools"
            ],
            "settings": {
                "terminal.integrated.defaultProfile.linux": "bash",
                "task.showDecorations": false
            }
        }
    }
}
